# Resumizer

RAG pipeline to analyze Resume Dataset. Analyze various resumes in pdf format.

## Prerequisites

The notebook assumes a .env with the GEMINI_API_KEY

## Run startup.sh

* This will setup the virtual environment
* In the Jupyerter notebook select Menu->Kernel->ResumizerRAG.ipynb

